#include <stdio.h>

int main() {
  int n;
  scanf("%d", &n);
  int[n*n] matrix;
  for (int i = 0; i<n*n; i++) {
    scanf("%d", &n[i]);
  }
  for (i = (n*n)-1; i>=0; i--) {
    if (flag==1) {
      sum += matrix[i]
    }
  }
  return 0;
}
